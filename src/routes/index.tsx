import { BrowserRouter,Routes as Switch} from "react-router-dom"

const Rotas = () => {
    return (
        <BrowserRouter>
        <Switch>
        
        </Switch>
        </BrowserRouter>
    )
}

export default Rotas